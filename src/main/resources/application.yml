spring:
  application:
    name: ai-code-helper
server:
  port: 8081
  servlet:
      context-path: /api


langchain4j:
  community:
    dashscope:
      chat-model:
        model-name: qwen-plus
        api-key: ${API-KEY}
      embedding-model:
        model-name: text-embedding-v4
        api-key: ${API-KEY}
      streaming-chat-model:
        model-name: qwen-plus
        api-key: ${API-KEY}
bigmodel:
  api-key: ${BIG-API-KEY}